import Layout from '../components/Layout';

function Events() {
  return (
    <Layout
      title="Events"
      subtitle="Create events, manage registrations, and track attendance."
    >
      <div className="empty-state">Event calendar and registration UI will go here.</div>
    </Layout>
  );
}

export default Events;
